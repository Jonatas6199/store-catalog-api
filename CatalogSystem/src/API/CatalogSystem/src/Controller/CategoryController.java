package Controller;

public class CategoryController {
	/*
	public Object selectCategory() {
		// ListArray<json> categories = CategoryDAO.Select();
		// return Methods.GetJsonArray(categories);
		return null;
	}
	
	public Object insertCategory(json categoryJson) {
		// category = Json.Create(categoryJson, Category)
		// CategoryDAO.insert(category);
		
		return "Deu certo";
	}
	
	public void updatecategory(json categoryJson) {
		// category = Json.Create(categoryJson, Category)
		// CategoryDAO.update(category);
	} */
}
